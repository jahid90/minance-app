version: '3'

networks:
  default:
    external:
      name: home_network

services:
  web:
    build: .
    image: transaction.react:local
    container_name: transaction-react
    restart: unless-stopped
    expose:
      - 80
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
